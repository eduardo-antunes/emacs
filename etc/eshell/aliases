# Abrindo o emacs pela eshell
alias ed find-file $1
alias ff find-file $1
alias emacs find-file $1

# Confirme antes de sobrescrever algo
alias rm rm -i $1
alias mv mv -i $1
alias cp cp -i $1

# Abrindo ferramentas úteis
alias magit magit .
alias dired dired .
alias dir dired
